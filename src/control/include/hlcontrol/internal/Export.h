#ifndef HL_EXPORT_H
#define HL_EXPORT_H

#include <hlaudio/hlaudio.h>
#include <string>
using namespace std;

class Export {
    private:

    public:
        Export(string targetDir);
};

#endif // HL_EXPORT_H